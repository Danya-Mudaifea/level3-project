apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: results
  namespace: test
spec:
  resources:
    inputs:
    - name: level3-project
      type: git
  steps:
  - name: run-kubectl
    image: lachlanevenson/k8s-kubectl
    command: ["kubectl"]
    args:
      - "describe"
      - "pods"
      - "-l"
      - "name=rabbitmq"
      - 
      - "grep Conditions -A 5"

